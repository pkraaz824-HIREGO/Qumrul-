# Production Environment Variables Template
# DO NOT commit actual production values to version control

# Server Configuration
NODE_ENV=production
PORT=5000

# MongoDB Configuration (Use MongoDB Atlas for production)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/elite-store?retryWrites=true&w=majority

# JWT Configuration (MUST use strong secret in production)
JWT_SECRET=CHANGE_THIS_TO_STRONG_RANDOM_STRING_IN_PRODUCTION
JWT_EXPIRE=7d

# CORS Configuration (Your production frontend URL)
CLIENT_URL=https://your-frontend-domain.com
